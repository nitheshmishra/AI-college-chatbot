const c = document.getElementById('rain'); if (c) { const x = c.getContext('2d'); c.width = innerWidth; c.height = innerHeight; let d = Array.from({ length: 120 }, () => ({ x: Math.random() * c.width, y: Math.random() * c.height, l: Math.random() * 20 + 10, s: Math.random() * 4 + 4 })); function a() { x.clearRect(0, 0, c.width, c.height); d.forEach(r => { x.strokeStyle = 'rgba(255,255,255,.3)'; x.beginPath(); x.moveTo(r.x, r.y); x.lineTo(r.x, r.y + r.l); x.stroke(); r.y += r.s; if (r.y > c.height) r.y = -20; }); requestAnimationFrame(a) } a(); }
